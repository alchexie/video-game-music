{
  "name": "@vgm/api",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "rimraf dist && tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/multipart": "^9.4.0",
    "@vgm/core": "workspace:*",
    "@vgm/shared": "workspace:*",
    "dotenv": "^16.6.1",
    "fastify": "^5.4.0",
    "uuid": "^11.1.0"
  }
}
